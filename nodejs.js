function add(a,b){
    return a + b;
}
function minus(a,b){
    return a - b;
}

module.exports = {add,minus}//this is short hand property, this can be use while the function name and object name are similar when you call as a obejct